CREATE USER kriso123;
CREATE DATABASE videa;
GRANT ALL PRIVILEGES ON DATABASE videa TO kriso123;

CREATE TABLE IF NOT EXISTS videos (
  video_id INT NOT NULL,
  title varchar(250) NOT NULL,
  title varchar(250) NOT NULL,
  title varchar(250) NOT NULL,
  title varchar(250) NOT NULL,
  
  PRIMARY KEY (product_id)
);